services:
  scidataseal:
        image: jodlidev/scidataseal:latest
        restart: "unless-stopped"
        volumes:
            - ./config:/SciDataSeal/config:rw
        ports:
            - "19419:19419"
